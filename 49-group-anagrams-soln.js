/**
 * @param {string[]} strs
 * @return {string[][]}
 */

// declare groupAnagrams, takes an array of strings as single param
var groupAnagrams = function (strs) {
  // declare count, an array that holds the char count of each word
};
